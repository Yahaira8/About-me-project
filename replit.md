# About Me website

## Run in Replit

The project is a Vite + React app. Its Replit web workflow runs:

```bash
npm run dev
```

The development server binds to `0.0.0.0:5000`, which makes the site available in Replit Preview.

## Other commands

```bash
npm run build
npm run lint
```